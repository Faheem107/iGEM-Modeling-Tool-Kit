node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Next.js build output & caches
.next/
out/
next-env.d.ts
tsconfig.tsbuildinfo

# Local-only project notes & references (kept on disk, kept off GitHub)
DESIGN.md
WRITING_STYLE.md
DESIGN_SYSTEM.md
CONTENT_AUDIT.md
UI_SKILLS_REFERENCE.md
WET_LAB_AND_DRY_LAB_APPROACH.md
PARAMETER_AUDIT_AND_PRESENTATION.md
# Working notes for the dust-exposure build. The modules they describe are
# shipped in src/components/exposure/ and their data provenance is in
# src/lib/moduleSources.ts, which is what a reader actually needs.
DUST_EXPOSURE_BUILD_PLAN.md
DUST_EXPOSURE_DATA_SOURCES.md
DUST_EXPOSURE_KNOWLEDGE_GRAPH.md
DUST_EXPOSURE_MODULE_SPEC.md
DUST_EXPOSURE_TRANSPORT_AND_VALUE.md
# Wind model validation. The scripts in scripts/verify_wind_*.py are the code and
# are pushed; these two are the write-up and the working notes, kept on disk.
WIND_PATTERN_TESTS_NEXT_STEPS.md
WIND_VALIDATION_RESULTS.md
Research Table iGEM 2026*.md
Business team plan.md
Toolkit in layman's terminology.md
modeling_subteam_theory.md
script.txt
# How the wind side of the business case was done, written for the team. The
# code it describes is pushed; the walkthrough stays on disk.
Wind model for business, in plain terms.txt
# Draft copy for the wiki, and a frame grabbed off the landing animation to
# transcribe it. Neither is code.
Model Wiki Page.md
beat1-transcription.png
# The business/narrative subteam's working document, and the screenshots taken
# while reviewing the landing animation. The sources it cites are recorded in
# src/lib/moduleSources.ts, which is what a reader actually needs.
Dune_ Stay at home.md
*.png
!public/**/*.png
!app/icon.png
skills-lock.json
manim_videos/README.md
project reframe/
docs/

# Machine-local assistant config, kept on disk. Only iGEM's
# .claude/RESPONSIBLE_AI_USE.md is tracked.
CLAUDE.md
AGENTS.md
.agents/
!.claude/
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/skills/
.mcp.json
cobrapy/

# Manim render artifacts (source scenes are kept; rendered media is regenerated)
manim_videos/media/
manim_videos/pdb/

# Python bytecode
manim_videos/__pycache__/
__pycache__/
*.pyc

# Playwright MCP artifacts
.playwright-mcp/

# Credentials, never commit. Real keys live outside the repo in ~/.cdsapirc and ~/.netrc
.cdsapirc
.netrc
.edl_token
*.pem
*secret*
*credential*

# Literature and source data kept on disk, kept off GitHub.
# references/ holds publisher PDFs from paywalled journals (Wiley, Springer, Elsevier).
# Committing them to a public repo would redistribute copyrighted material.
# Everything the build needs is regenerated into public/data/ by scripts/.
references/

# Local scratch
.playwright-mcp/
.era5_cache/
# Resume cache for scripts/fetch_pv_climatology.py, deleted on a successful run
.pv_cache.json
.cams_cache.json

# npm cache, written by CI
.npm/

# Browser-check scratch output
shots/
.merra2_cache.json

# Its own repository, pushed to github.com/Faheem107/model-wiki
model-wiki/

# Resume cache for scripts/verify_wind_stations.py (METAR from the IEM archive)
.metar_cache/

# The unedited iGEM template README. README.md replaces it.
README_igem_original.md

# Business-team market sizing (GEM Global Solar Power Tracker via an R script
# kept elsewhere). scripts/pv_site_drift.py reads drylab_sites_gcc.csv from here
# and writes only the derived wind statistics to public/data/.
Solar Panel output/
